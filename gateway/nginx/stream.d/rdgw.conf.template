resolver 127.0.0.11 valid=30s;
resolver_timeout 5s;

server {
  listen 443;
  # Use a variable to avoid resolving at config load time.
  set $rdp_upstream "__RDPGW_UPSTREAM__";
  proxy_pass $rdp_upstream;
  proxy_connect_timeout 30s;
  proxy_timeout 1h;
}
